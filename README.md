REST API with CRUD operations towards an in-memory H2 database, on the Book entity
And security with 2 roles (user and admin), implementing cache and a programmed task to show the number of books in DB.
